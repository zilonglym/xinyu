package com.graby.store.admin.web.store;

import java.util.List;
import java.util.Map;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;

import com.graby.store.admin.web.BaseController;
import com.graby.store.entity.store.UserShop;

/**
 * 商品同步操作控制类
 * @author yangmin
 *
 */
@Controller
@RequestMapping(value="syncItem")
public class ItemWebController extends BaseController {
	/*
	@Autowired
	private UserShopService shopService;
	@Autowired
	private SyncTopService syncTopService;
	@Autowired
	private SyncTopTradeService syncTradeService;
	/**
	 * 转向页面
	 * @return
	@RequestMapping("toSync")
	public String toSync(){
		return "";
	}
	
	@RequestMapping(value="sync")
	@ResponseBody
	public Map<String,Object> sync() throws Exception{
	//	Account account=SessionUser.get();
		//User user=account.getUser();
		List<UserShop> list=this.shopService.getUserShopListByUserId("1");
		for(int i=0;list!=null && i<list.size();i++){
			UserShop shop=list.get(i);
			System.err.println(shop.getSessionKey());
			System.err.println(this.syncTopService.getNick(shop.getSessionKey()));
			System.err.println(this.syncTopService.syncItem(shop.getSessionKey()));
		}
		return null;
	}
*/
}
